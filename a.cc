/**
 * common header
 */
#include <iostream>
using namespace std;

int main()
{
    int n;
    while(cin >> n && n)
    {
        for(int i = 0; i < n; i++)
        {
            
        }
    }
    return 0;
}
